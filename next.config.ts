/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export", // enables static export
  basePath: "/junoocha.github.io", // your repo name
  images: { unoptimized: true },
};

export default nextConfig;
